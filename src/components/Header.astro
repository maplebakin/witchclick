---
import HeaderIsland from "@/components/HeaderIsland.tsx";
import MobileDrawer from "@/components/MobileDrawer.tsx";
import SearchPanel from "@/components/SearchPanel.tsx";

const nav = [
  { href: "/rituals/", label: "Rituals" },
  { href: "/grimoire/", label: "Entity Library" },
  { href: "/tools/", label: "Tools & Prints" },
  { href: "/about/", label: "About" },
  { href: "/contact/", label: "Contact" },
];
---
<header class="sticky top-0 z-50 border-b bg-white/80 backdrop-blur dark:bg-zinc-950/80">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-3">
    <a href="/" class="text-xl font-bold">WitchClick</a>
    <nav class="hidden items-center gap-6 md:flex">
      {nav.map((item) => (
        <a href={item.href} class="hover:underline">
          {item.label}
        </a>
      ))}
    </nav>
    <div class="md:hidden">
      <HeaderIsland client:load />
    </div>
  </div>
  <MobileDrawer nav={nav} client:load />
  <SearchPanel label="Search posts" client:load />
</header>
